import Cell from './Cell'

const Block = ({ fixedCells, grid, handleSelect }) => 
    <div>
        {/* {blockIndices.map((row, rowIndex) => {
            {row.map((gridIndex, colIndex) => {
                <Cell 
                    value={grid[gridIndex[0]][gridIndex[1]]}
                />
            })}
        })} */}

    </div>

export default Block